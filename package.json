{
  "private": true,
  "type": "module",
  "scripts": {
    "stats": "node scripts/generate-stats.js",
    "update-latest": "node scripts/update-latest-articles.js",
    "docs:dev": "npm run update-latest && npm run stats && vitepress dev docs",
    "docs:build": "npm run update-latest && npm run stats && vitepress build docs",
    "docs:preview": "vitepress preview docs --port 8080"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vitepress-sidebar": "^1.33.1"
  },
  "dependencies": {
    "markdown-it-mathjax3": "^4.3.2"
  }
}
